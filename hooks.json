{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "if [ -d docs/coding-sessions ]; then latest=$(ls -t docs/coding-sessions/*_session-summary.md 2>/dev/null | head -n 1); if [ -n \"$latest\" ]; then echo \"Context from previous session ($(basename \"$latest\")):\"; awk '/^## Pending Tasks/{flag=1; print; next} /^##/{flag=0} flag' \"$latest\"; fi; fi"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "input=$(cat); re='\"command\":[[:space:]]*\"[^\"]*git[[:space:]]+commit'; if [[ \"$input\" =~ $re ]]; then echo 'Reminder: Use commit-assistant for proper commit formatting.' >&2; fi; exit 0"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "input=$(cat); re='\"command\":[[:space:]]*\"[^\"]*git[[:space:]]+push'; if [[ \"$input\" =~ $re ]]; then echo 'Reminder: Run push-assistant agent before pushing code.' >&2; fi; exit 0"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "input=$(cat); re='\"command\":[[:space:]]*\"[^\"]*gh[[:space:]]+pr[[:space:]]+create'; if [[ \"$input\" =~ $re ]]; then echo 'Reminder: Consider running ami-pr-conflict-detector or ami-pr-publisher before creating PR.' >&2; fi; exit 0"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "input=$(cat); file=\"${input#*file_path\": \"}\"; file=\"${file%%\"*}\"; if [ -n \"$file\" ] && [ -f \"$file\" ]; then if grep -qE 'console\\.log|debugger|TODO|FIXME' \"$file\"; then echo \"Warning: Detected debug statements or TODOs in $file. Review before commit.\" >&2; fi; fi"
          }
        ]
      }
    ]
  }
}
